#pragma once
#include "simpleGUI.h"
/*
class ListScroller
{
public:
	ListScroller();
	~ListScroller();
	void update(WMInterfaceData& wm_dat, RenderWindow& window);
	void draw(RenderTarget* target);

	void setSize(Point _size);
	void setPosition(Point _position);
	Point getSize();
	Point getPosition();
};
class ListItem
{
public:
	ListItem();
	ListItem(BoundingBox _box);
	~ListItem();
	Text txt;
	RectangleShape rect;
	BoundingBox box;
};
class GeneralList
{
public:
	GeneralList();
	GeneralList(BoundingBox _box);
	~GeneralList();
	void update(WMInterfaceData& wm_dat, RenderWindow& window);
	void draw(RenderTarget* target);
private:
	bool have_a_scroller;
	Texture texture;
	BoundingBox box;
};
*/